@AGENTS.md

## Workflow vocabulary

- **"Ship"** (or "Ship it") = the full delivery flow: stage all changes →
  commit with a clear message → push the branch → open the pull request. Report
  back when it's merged and green. This is a complete, tested unit of work going
  to `main` — treat it with that weight, not as a chore.
